//conf/dev.config

conda {
    enabled = true
    create = true // Prevent environment creation in dev mode
    cacheDir = 'conda_cache' // Optional: cache Conda environments
}
params {
    conda_loc = ''
    dev_mode = true
}